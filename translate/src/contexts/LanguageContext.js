 // create a context object
 // The only reason we are creating this context object is so that we could import it only on those components and use it.

 import React from 'react';

export default React.createContext('english');